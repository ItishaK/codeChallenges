# This is a sample Python script.

# Press Shift+F10 to execute it or replace it with your code.
# Press Double Shift to search everywhere for classes, files, tool windows, actions, and settings.
import subprocess


def print_hi(name):
    # Use a breakpoint in the code line below to debug your script.
    print(f'Hi, {name}')  # Press Ctrl+F8 to toggle the breakpoint.


# Press the green button in the gutter to run the script.
if __name__ == '__main__':
    # print_hi('PyCharm')
    # subprocess.run(["python", "ddlScript.py"])
     #subprocess.run(["python", "alternateString.py"])
    # subprocess.run(["python", "longestNiceSubString.py"])
    # subprocess.run(["python","swapLinkedList.py"])
    # subprocess.run(["python", "longestSubstring.py"])
     #subprocess.run(["python", "groupAnagrams.py"])
    # subprocess.run(["python", "generateParantheses.py"])
    # subprocess.run(["python", "sortColors.py"])
    #subprocess.run(["python", "sortEmailsWithDomains.py"])
    #subprocess.run(["python", "testing.py"])
    #subprocess.run(["python", "searching2DMatrix.py"])
    #subprocess.run(["python", "searching2DMatrix.py"])
    #subprocess.run(["python", "practiceTest1.py"])
    #subprocess.run(["python", "practiceTest2.py"])
    # subprocess.run(["python", "pseudo_palindromic_path.py"])
    #subprocess.run(["python", "permutations.py"])
    #subprocess.run(["python", "longCommonSubseq.py"])
    # subprocess.run(["python", "mostFreqChar.py"])
    subprocess.run(["python", "reverseInt.py"])



# See PyCharm help at https://www.jetbrains.com/help/pycharm/
